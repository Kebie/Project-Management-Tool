<h1>All Projects</h1>


<table class="table">
  <tr>
    <th>Project Title</th>
    <th>Due Date</th>
  </tr>
  <% @projects.each do |project| %>
    <tr>
      <td><%= link_to project.title, project %></td>
      <td><%= project.due_date.strftime("%F")  %></td>
    </tr>
  <% end %>
</table>